# Pomocne skripty sa nesmu volat priamo cez HTTP — spoliehaju sa na to, ze ich
# nacita index.php, ktory uz ma nacitanu konfiguraciu.
# PHP require ide cez filesystem, toto pravidlo mu nebrani.
Require all denied
